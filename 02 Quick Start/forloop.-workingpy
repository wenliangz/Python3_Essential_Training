#!/usr/bin/python3

# read the lines from the file
fh = open('lines.txt')
# for line in fh.readlines():
#     print(line, end='')

# Readlines() is iterator function that convert iterables into iterator that is suitable for use in for loop.
# Readlines can can be replaced by more generic function: iter()

it=iter(fh)
for line in it:
    print (line, end='')
